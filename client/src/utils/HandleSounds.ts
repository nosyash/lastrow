// import notification1 from '../sounds/notification1.mp3';
const notification2 = require('../sounds/notification2.mp3');

const sound = new Audio(notification2);
export default function playSound() {
    // if (!sound.paused) sound.pause();
    // sound.currentTime = 0;
    // sound.volume = 0.5;
    // sound.play();
}
